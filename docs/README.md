# Screenshots for README

Place the following images here so they appear in the main README:

- `screenshot_main.png` — Main interface (map + sidebar)
- `screenshot_mobile.png` — Mobile scenario (same node ID, time steps)
- `screenshot_fixed.png` — Fixed scenario (different node IDs, time 0)
- `screenshot_output.png` — positions.dat output and Copy/Save buttons

You can take screenshots from `cooja_positioner_last_point_circle.html` and name them as above.
